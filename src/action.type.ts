import RouteType from './routing/route.type';
import RouteParamsType from './routing/route.params.type';

type ActionType = {
  route: RouteType;
  params: RouteParamsType;
};

export default ActionType;
